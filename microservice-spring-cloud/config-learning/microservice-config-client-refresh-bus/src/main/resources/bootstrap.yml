spring:
  cloud:
    config:
      uri: http://localhost:8080
  application:
    name: microservice-config-client-refresh
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest